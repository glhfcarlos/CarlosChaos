import { defineConfig } from 'vite';

export default defineConfig({
  base: '/CarlosChaos/', // Replace 'repository-name' with your actual GitHub repository name
  build: {
    outDir: 'dist', // This is the default output folder for Vite builds
  },
});
