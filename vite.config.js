import { defineConfig } from 'vite';

export default defineConfig({
  base: '/CarlosChaos/',  // Replace with your repository name
  build: {
    outDir: 'dist', // This is the default output folder for Vite builds
  },
});
